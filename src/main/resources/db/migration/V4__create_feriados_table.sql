CREATE TABLE feriados (
                          id BIGINT AUTO_INCREMENT PRIMARY KEY,
                          data DATE NOT NULL,
                          nome VARCHAR(255),
                          tipo VARCHAR(255)
);